mysql server  < - 프로토콜 ->  client
oracle serever < - 프로토콜 ->  client  

프로토콜이 둘이다름 그래서 

인터페이스를 정의를했다.[connect, result --등의 메소드를가진(jdbc API) ]

=
 mysql [ jdbc 드라이버(라이브러리 [클래스들이있음])] | ┐  
 oracle [ jdbc 드라이버(라이브러리 [클래스들이있음])] |   ┐
                                                                 각각 인터페이스를 구현함 		  

(즉 디비가 뭐든 인터페이스에만 접근하면된다)
-------
 그래서 사용자들은 드라이버만 가져와서 
 인터페이스를 사용해서 쓰면된다.
  
 *즉 JDBC API만 익히면된다.*

---------------------------------------
  
 이클립스에서 유저라이브러리등록 후 사용해야함 
 *이클립스에서 쿼리날릴때 세미콜론 ; 빼세요 		
     
 --문제는 자바안에 쿼리가들어있다는거임 
   긴쿼리가오면 난장판된다.
   
   이문제는 스프링에서 해결한다. 

------------------
select

 1) JDBC Driver(MySQL) 로딩 - 라이브러리 등록 후 해야함		
 2) 연결하기   - 프로토콜  htpp같은-  	--연결 : 커넥션객체 얻어오기
 3) Statement 객체를 생성 - 쿼리를 떄리기 위해 
 4) SQL문 실행 
 5) 결과 가져오기

 5)결과가져오기 : ResultSet rs

  rs.next를해줘야함 rs가 가르키고있는걸 계속 next로 커서이동을해줘야함 
   
  첫 rs.next = 첫로우

-----

 Class.forName("com.mysql.jdbc.Driver"); 원리알기 
 디비의 드라이버를개발 간접경험해보자
 = package com.douzone.jdbc.driver;

 

 
 
 