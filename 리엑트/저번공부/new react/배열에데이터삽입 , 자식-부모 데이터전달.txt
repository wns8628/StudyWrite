
- App컴포넌트 내부에서  handleCreate() 메소드를 만듬 

- 만든메소드를 자식컴포넌트에게 props로 전달.

- 자식컴포넌트는 전달된 메소드를 호출시켜서 데이터값이 부모(app)한테 들어가게한다.

 배열넣기 : information : information.concat(Object.assign({},data, {id:this.id++})) //이런방법등등많다.


---------------

 배열렌더링할때 키를쓰는이유 : 성능을위해 , 키가있어야 데이터삽입삭제가 용이 

---------
 배열에서 데이터 제거하기

     배열 내장함수 slice  - (0,2) 이런식으로 잘라냄 0번쨰부터 2번째앞에까지 뽑는다. 불변배열함수로 원본놔줌 디비셀렉트처럼? ㅋㅋ
     배열 내장함수 filter - 안에 함수를써서 필터해낸다. 불변배열로 원본은놔둠